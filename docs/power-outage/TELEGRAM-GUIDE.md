# 📱 Как отправить сообщение в Telegram

## Быстрый способ

### 1. Тестовое сообщение

```bash
php artisan telegram:send-test
```

Отправит красиво оформленное тестовое сообщение с подтверждением, что бот работает.

### 2. Своё сообщение

```bash
php artisan telegram:send-test "Привет из Laravel!"
```

### 3. Отправка уведомления о графике

**Самый простой способ:**
```bash
# Отправить уведомление о последнем графике
php artisan power:notify
```

**Альтернативные способы:**
```bash
# Отправить уведомление о графике на конкретную дату
php artisan power:notify 09-11-2025

# Получить график И отправить уведомление
php artisan power:fetch-schedule --notify

# Через tinker (для разработки)
php artisan tinker --execute="App\Jobs\SendPowerOutageNotification::dispatch(App\Models\PowerOutageSchedule::latest()->first());"
```

## Примеры

### Простое сообщение

```bash
php artisan telegram:send-test "Тест системы мониторинга"
```

### С HTML форматированием

```bash
php artisan telegram:send-test "<b>Внимание!</b> Обновление графика"
```

### Многострочное сообщение

```bash
php artisan telegram:send-test "Первая строка
Вторая строка
Третья строка"
```

## Проверка настроек

### 1. Проверить переменные окружения

```bash
grep TELEGRAM .env
```

Должно вывести:
```
TELEGRAM_BOT_TOKEN=ваш_токен
TELEGRAM_CHAT_ID=ваш_chat_id
```

### 2. Проверить конфигурацию

```bash
php artisan tinker --execute="
echo 'Bot Token: ' . config('services.telegram.bot_token') . PHP_EOL;
echo 'Chat ID: ' . config('services.telegram.chat_id') . PHP_EOL;
"
```

## Устранение проблем

### Сообщения не приходят

1. **Проверьте токен бота:**
   ```bash
   curl "https://api.telegram.org/bot$(grep TELEGRAM_BOT_TOKEN .env | cut -d'=' -f2)/getMe"
   ```

2. **Проверьте chat_id:**
   - Отправьте любое сообщение боту
   - Откройте: `https://api.telegram.org/botВАШ_ТОКЕН/getUpdates`
   - Найдите ваш chat_id в ответе

3. **Проверьте логи:**
   ```bash
   tail -f storage/logs/laravel.log | grep -i telegram
   ```

### Ошибка "Unauthorized"

Неправильный токен бота. Проверьте `TELEGRAM_BOT_TOKEN` в `.env`

### Ошибка "Chat not found"

Неправильный chat_id или бот не добавлен в чат/группу. Проверьте `TELEGRAM_CHAT_ID` в `.env`

## Программная отправка

### Из контроллера или команды

```php
use Illuminate\Support\Facades\Http;

$botToken = config('services.telegram.bot_token');
$chatId = config('services.telegram.chat_id');

$response = Http::post("https://api.telegram.org/bot{$botToken}/sendMessage", [
    'chat_id' => $chatId,
    'text' => 'Ваше сообщение',
    'parse_mode' => 'HTML',
]);
```

### Через Job

```php
use App\Jobs\SendPowerOutageNotification;
use App\Models\PowerOutageSchedule;

$schedule = PowerOutageSchedule::latest()->first();
SendPowerOutageNotification::dispatch($schedule);
```

### Через уведомление (Notification)

Создайте Notification класс для более гибкого управления уведомлениями.

## Форматирование сообщений

Telegram поддерживает HTML и Markdown:

### HTML (рекомендуется)

```php
$message = "<b>Жирный</b>\n";
$message .= "<i>Курсив</i>\n";
$message .= "<code>Код</code>\n";
$message .= "<a href='https://example.com'>Ссылка</a>";
```

### Emoji

```php
$message = "✅ Успешно\n";
$message .= "❌ Ошибка\n";
$message .= "⚠️ Внимание\n";
$message .= "🔌 Отключение\n";
$message .= "📊 Статистика";
```

## Полезные команды

```bash
# Отправить тестовое сообщение
php artisan telegram:send-test

# Отправить своё сообщение
php artisan telegram:send-test "Текст"

# Получить информацию о боте
curl "https://api.telegram.org/botВАШ_ТОКЕН/getMe"

# Получить последние обновления
curl "https://api.telegram.org/botВАШ_ТОКЕН/getUpdates"
```

## Автоматические уведомления

### Как это работает

Система автоматически отправляет уведомления в Telegram в следующих случаях:

1. **При первом получении графика** - когда график для даты получен впервые
2. **При изменении графика** - когда обнаружены изменения в расписании отключений
3. **Принудительно** - когда используется флаг `--notify`

### Примеры

```bash
# Получить график - уведомление отправится автоматически при изменениях
php artisan power:fetch-schedule

# Получить график и принудительно отправить уведомление
php artisan power:fetch-schedule --notify

# Отправить уведомление о последнем графике
php artisan power:notify
```

### Автоматическая проверка каждые 10 минут

Уведомления отправляются автоматически при обнаружении изменений через cron (каждые 10 минут).

Для настройки cron см. [QUICKSTART.md](QUICKSTART.md)

### Логика определения изменений

Система сравнивает MD5 хеш `schedule_data` (детального расписания отключений):
- Если хеш отличается = график изменился → отправляется уведомление
- Если хеш совпадает = без изменений → уведомление не отправляется
- С флагом `--notify` = уведомление отправляется всегда

---

**Готово!** Теперь вы можете отправлять сообщения в Telegram! 🎉


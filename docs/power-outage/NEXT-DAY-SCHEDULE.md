# Автоматична перевірка графіка на наступний день

## Огляд

Додано автоматичну перевірку графіків відключень на **наступний день** з випадковим інтервалом від **30 до 60 хвилин**. Це дозволяє завчасно отримувати інформацію про майбутні відключення.

## Як працює

### Базова частота: кожні 30 хвилин

Завдання запланов ano на виконання кожні 30 хвилин:

```php
->cron('*/30 * * * *')
```

### Рандомізація: пропуск з вірогідністю 50%

Додано `skip()` функцію, яка пропускає виконання з вірогідністю 50%:

```php
->skip(function () {
    return rand(0, 1) === 0; // 50% шанс пропуску
})
```

### Результат: випадковий інтервал 30-60 хвилин

- Якщо **не пропустив** (50%) → виконується через 30 хв
- Якщо **пропустив** (50%) → виконається через 60 хв
- **Середній інтервал:** ~45 хвилин

## Налаштування в routes/console.php

```php
// Проверка графика на следующий день с случайным интервалом от 30 до 60 минут
Schedule::call(function () {
    \Illuminate\Support\Facades\Artisan::call('power:fetch-schedule', [
        'date' => now()->addDay()->format('d-m-Y'),
    ]);
})
    ->cron('*/30 * * * *') // Каждые 30 минут
    ->skip(function () {
        // Пропускаем выполнение с вероятностью 50% для рандомизации (среднее ~45-60 мин)
        return rand(0, 1) === 0;
    })
    ->name('power:fetch-schedule-next-day')
    ->withoutOverlapping()
    ->onOneServer();
```

## Переваги

### 1. Завчасне отримання інформації

Як тільки ДТЕК опублікує графік на наступний день - система автоматично його отримає та відправить сповіщення.

### 2. Випадковий інтервал

Рандомізація допомагає:
- Уникнути передбачуваних patterns запитів
- Зменшити ймовірність блокування
- Розподілити навантаження на сервер

### 3. Оптимізація трафіку

Завдяки двоетапній перевірці через `unloading-info.php`:
- Легкі запити кожні 30-60 хв (~2 KB)
- HTML завантажується тільки коли графік з'явиться

### 4. Автоматичні сповіщення

Коли графік на наступний день опублікується - система:
- Завантажить HTML
- Парситиме дані
- Збереже в БД
- Відправить сповіщення в Telegram

## Приклад роботи

### Сценарій: Сьогодні 10.11.2025, графік на 11.11 ще не опублікований

```
12:00 → Перевірка на 11-11-2025 → API порожній → Пропуск
12:30 → Пропуск (rand 50%)
13:00 → Перевірка на 11-11-2025 → API порожній → Пропуск
13:30 → Пропуск (rand 50%)
14:00 → Перевірка на 11-11-2025 → API порожній → Пропуск
...

19:00 → Перевірка на 11-11-2025 → API повернув дані! ✅
        → created_date: "10-11-2025 18:55"
        → Завантаження HTML
        → Парсинг
        → Збереження в БД
        → 📱 Telegram сповіщення: "Графік на 11.11 опублікований!"

19:30 → Пропуск (rand 50%)
20:00 → Перевірка на 11-11-2025 → Дата не змінилась → Пропуск HTML ✅
```

## Статистика

### За день (48 можливих запусків кожні 30 хв):

**Без рандомізації:**
- 48 перевірок × 2 KB = **96 KB/день** (до публікації графіка)

**З рандомізацією (50% пропусків):**
- ~24 перевірки × 2 KB = **~48 KB/день** (до публікації графіка)
- **Економія:** ~50%

### Після публікації графіка:

Завдяки перевірці `created_date`:
- Легкі запити продовжуються (~2 KB)
- HTML більше не завантажується
- Трафік залишається мінімальним

## Поточний scheduler

```bash
php artisan schedule:list
```

**Вивід:**
```
*/3  *   * * *  php artisan power:fetch-schedule .............. Next Due: за 2 хвилини
*/30 *   * * *  power:fetch-schedule-next-day ................ Next Due: за 11 хвилин
```

## Тестування

### Вручну перевірити графік на наступний день:

```bash
php artisan power:fetch-schedule $(date -d '+1 day' '+%d-%m-%Y') -v
```

**Приклад виводу (графік ще не опублікований):**
```
Получение графика отключений на 11-11-2025...
Проверка наличия обновлений...
API не вернул данных. Возможно, график ещё не опубликован.
Получен пустой ответ от сервера. Возможно, данные для даты 11-11-2025 ещё не доступны.
```

✅ **Це нормально!** Команда спрацює автоматично, коли дані з'являться.

### Перевірити scheduler:

```bash
php artisan schedule:list
```

### Примусово запустити scheduler:

```bash
php artisan schedule:run -v
```

## Налаштування інтервалу

### Змінити базову частоту

У `routes/console.php`:

```php
->cron('*/30 * * * *') // Кожні 30 хв (поточне)
->cron('*/20 * * * *') // Кожні 20 хв
->cron('*/45 * * * *') // Кожні 45 хв
->cron('0 * * * *')    // Щогодини
```

### Змінити ймовірність пропуску

```php
// Поточне: 50% пропуск
->skip(function () {
    return rand(0, 1) === 0;
})

// 33% пропуск (швидші перевірки)
->skip(function () {
    return rand(0, 2) === 0;
})

// 66% пропуск (рідші перевірки)
->skip(function () {
    return rand(0, 2) !== 0;
})

// Без рандомізації (точний інтервал)
// Просто видалити ->skip()
```

### Поточне налаштування

| Параметр | Значення |
|----------|----------|
| Базова частота | 30 хв |
| Ймовірність пропуску | 50% |
| Мінімальний інтервал | 30 хв |
| Максимальний інтервал | 60 хв |
| Середній інтервал | ~45 хв |

## Моніторинг

### Перегляд логів

```bash
tail -f storage/logs/laravel.log | grep "11-11-2025"
```

### Перевірка БД

```bash
php artisan tinker
>>> App\Models\PowerOutageSchedule::where('schedule_date', '2025-11-11')->first()
```

### Статистика перевірок

Scheduler логує кожен запуск у `storage/logs/laravel.log`.

## FAQ

### Q: Чому не використовується `->daily()` або `->hourly()`?

**A:** Нам потрібен **випадковий інтервал** для:
- Уникнення передбачуваних patterns
- Зменшення ризику блокування
- Більш природної поведінки

### Q: Чому closure, а не `Schedule::command()`?

**A:** Дата наступного дня обчислюється **динамічно** при кожному запуску:

```php
now()->addDay()->format('d-m-Y')
```

Це гарантує, що завжди перевіряється саме наступний день.

### Q: Що якщо графік публікується пізно ввечері?

**A:** Система продовжуватиме перевірки протягом всієї доби. Як тільки графік з'явиться - його буде отримано та надіслано сповіщення.

### Q: Чи можна перевіряти графіки на 2-3 дні вперед?

**A:** Так, можна додати додаткові завдання:

```php
// На 2 дні вперед
Schedule::call(function () {
    Artisan::call('power:fetch-schedule', [
        'date' => now()->addDays(2)->format('d-m-Y'),
    ]);
})->cron('0 */2 * * *') // Кожні 2 години
  ->name('power:fetch-schedule-2-days')
  ->withoutOverlapping()
  ->onOneServer();
```

## Технічні деталі

### Чому не `->runInBackground()`?

Closure (анонімні функції) в Laravel Scheduler **не можуть** виконуватись у фоні:

```php
// ❌ Помилка:
Schedule::call(function () { ... })->runInBackground();
// RuntimeException: Scheduled closures can not be run in the background.

// ✅ Правильно:
Schedule::call(function () { ... });
```

Для команд можна використовувати `->runInBackground()`:

```php
Schedule::command('power:fetch-schedule')->runInBackground();
```

### Чому `->name()` перед `->withoutOverlapping()`?

У Laravel 12 `name()` повинен бути встановлений **перед** `withoutOverlapping()`:

```php
// ✅ Правильно:
->name('my-task')
->withoutOverlapping()

// ❌ Помилка:
->withoutOverlapping()
->name('my-task')
// A scheduled event name is required to prevent overlapping.
```

## Підсумок

✅ Автоматична перевірка графіка на наступний день  
✅ Випадковий інтервал 30-60 хвилин  
✅ Мінімальний трафік завдяки API оптимізації  
✅ Автоматичні сповіщення при публікації  
✅ Працює цілодобово  
✅ Не створює передбачуваних patterns  

**Створено:** 10.11.2025  
**Версія:** 2.1.0  
**Статус:** ✅ Працює


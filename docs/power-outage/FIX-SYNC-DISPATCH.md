# Исправление: Сообщения теперь отправляются немедленно

## Проблема

Команды отправляли Job в очередь (`dispatch()`), но уведомления не приходили в Telegram, потому что:
- Job добавлялся в очередь базы данных
- Для выполнения нужен был `queue:work`
- Без запущенного worker'а сообщения не отправлялись

## Решение

Изменено на синхронное выполнение (`dispatchSync()`):
- Уведомление отправляется **немедленно**
- Не требуется запуск `queue:work`
- Команда ждет завершения отправки

## Что изменено

### 1. FetchPowerOutageSchedule.php

**Было:**
```php
SendPowerOutageNotification::dispatch($schedule);
```

**Стало:**
```php
SendPowerOutageNotification::dispatchSync($schedule);
```

### 2. SendPowerOutageNotificationCommand.php

**Было:**
```php
SendPowerOutageNotification::dispatch($schedule);
```

**Стало:**
```php
SendPowerOutageNotification::dispatchSync($schedule);
```

## Результат

✅ **Теперь работает немедленно:**

```bash
$ php artisan power:notify
📤 Отправка уведомления о графике...
📅 Дата: 09.11.2025
🕒 Получен: 09.11.2025 16:00:39
✅ Уведомление отправлено в Telegram!
# → Сообщение СРАЗУ отправлено в Telegram!
```

```bash
$ php artisan power:fetch-schedule --notify
Получение графика отключений на 09-11-2025...
Данные не изменились
Принудительная отправка уведомления (флаг --notify)...
# → Сообщение СРАЗУ отправлено в Telegram!
```

## Проверка

Проверьте логи:
```bash
tail -f storage/logs/laravel.log | grep "Power outage"
```

Должны видеть:
```
[2025-11-09 16:03:08] local.INFO: Power outage notification sent to Telegram {"schedule_id":15}
```

## Важно

- `dispatchSync()` - выполняет Job **сразу** (синхронно)
- `dispatch()` - добавляет Job в **очередь** (требует worker)

Для команд, которые запускаются вручную или через cron, лучше использовать `dispatchSync()` для немедленной отправки.

## Тестирование

Все способы теперь работают немедленно:

```bash
# 1. Простая отправка
php artisan power:notify
# ✅ Работает сразу!

# 2. С получением графика
php artisan power:fetch-schedule --notify
# ✅ Работает сразу!

# 3. Тестовое сообщение
php artisan telegram:send-test "Привет!"
# ✅ Работает сразу!
```

---

**Проблема решена! Проверьте ваш Telegram - все сообщения должны прийти!** 🎉


# 🔔 Как отправляются уведомления в Telegram при изменениях

## ✅ Автоматическая отправка

Система **автоматически** отправляет уведомления в Telegram когда:

### 1. Первое получение графика
```bash
php artisan power:fetch-schedule
```
При первом получении графика на дату → **отправляет уведомление**

### 2. Обнаружены изменения в графике
```bash
php artisan power:fetch-schedule
```
Если график изменился (другой hash) → **отправляет уведомление**

### 3. Данные не изменились
```bash
php artisan power:fetch-schedule
```
Если график такой же (тот же hash) → **НЕ отправляет уведомление**

## 🚀 Ручная отправка

### Способ 1: Команда power:notify (САМЫЙ ПРОСТОЙ)

```bash
# Отправить уведомление о последнем графике
php artisan power:notify

# Отправить уведомление о графике на конкретную дату
php artisan power:notify 09-11-2025
```

### Способ 2: Флаг --notify

```bash
# Получить график и принудительно отправить уведомление
php artisan power:fetch-schedule --notify
```

Отправит уведомление даже если данные не изменились!

### Способ 3: Тестовое сообщение

```bash
php artisan telegram:send-test "Своё сообщение"
```

## 📊 Как работает определение изменений

```
1. Получаем HTML с сервера
2. Парсим в schedule_data (расписание отключений)
3. Генерируем MD5 hash из schedule_data
4. Сравниваем с hash последней записи в БД
   
   ├─ Hash отличается → ИЗМЕНЕНИЯ → Сохраняем + Отправляем уведомление
   ├─ Hash совпадает → БЕЗ ИЗМЕНЕНИЙ → Не отправляем
   └─ Первая запись → Сохраняем + Отправляем уведомление
```

## 🔄 Автоматическая проверка каждые 10 минут

После настройки cron, система автоматически:

```bash
* * * * * cd /path/to/project && php artisan schedule:run >> /dev/null 2>&1
```

Каждые 10 минут:
1. Получает график отключений
2. Проверяет на изменения
3. При изменениях → отправляет уведомление в Telegram

## 📝 Примеры

### Пример 1: Изменений нет

```bash
$ php artisan power:fetch-schedule
Получение графика отключений на 09-11-2025...
Данные не изменились
```
**Результат:** Уведомление НЕ отправлено ❌

---

### Пример 2: Обнаружены изменения

```bash
$ php artisan power:fetch-schedule
Получение графика отключений на 09-11-2025...
График отключений сохранён (ID: 10)
Обнаружены изменения в графике. Отправка уведомления...
```
**Результат:** Уведомление отправлено ✅

---

### Пример 3: Принудительная отправка

```bash
$ php artisan power:fetch-schedule --notify
Получение графика отключений на 09-11-2025...
Данные не изменились
Принудительная отправка уведомления (флаг --notify)...
```
**Результат:** Уведомление отправлено ✅

---

### Пример 4: Просто отправить уведомление

```bash
$ php artisan power:notify
📤 Отправка уведомления о графике...
📅 Дата: 09.11.2025
🕒 Получен: 09.11.2025 15:55:05
✅ Уведомление отправлено в Telegram!
```
**Результат:** Уведомление отправлено ✅

## 🎯 Краткая шпаргалка

| Действие | Команда | Когда отправляет |
|----------|---------|------------------|
| Получить график | `php artisan power:fetch-schedule` | При изменениях / первый раз |
| Получить + принудительно отправить | `php artisan power:fetch-schedule --notify` | Всегда |
| Отправить уведомление | `php artisan power:notify` | Всегда |
| Отправить тест | `php artisan telegram:send-test` | Всегда |

## ✅ Проверка

Проверьте, что уведомление пришло в ваш Telegram чат! 

Если не пришло:
1. Проверьте логи: `tail -f storage/logs/laravel.log | grep -i telegram`
2. Проверьте настройки: `grep TELEGRAM .env`
3. Отправьте тест: `php artisan telegram:send-test`

---

**Готово!** Теперь вы знаете как работают уведомления! 🎉


# Мониторинг графика отключений электроэнергии

## Описание

Система автоматического мониторинга и уведомлений о графиках отключений электроэнергии (ГПВ) для Полтавской области.

## Возможности

- 🔄 Автоматическое получение данных каждые 10 минут
- 📊 Парсинг HTML расписания в структурированные данные
- 💾 Сохранение истории изменений в базе данных
- 🔔 Уведомления в Telegram при изменении графика
- 📈 Анализ расписания по очередям и временным интервалам

## Структура данных

### Модель PowerOutageSchedule

```php
[
    'schedule_date' => '2025-11-08',           // Дата графика
    'description' => 'У зв\'язку зі...',       // Текстовое описание
    'periods' => [                              // Периоды отключений
        [
            'from' => '07:00',
            'to' => '16:00',
            'queues' => 2.5
        ],
        // ...
    ],
    'schedule_data' => [                        // Детальное расписание
        [
            'queue' => '1 черга',
            'subqueue' => '1',
            'hourly_status' => [                // 48 элементов (24 часа по 2 получаса)
                'on',    // Свет есть
                'off',   // Свет выключен
                'maybe', // Возможно отключение
                // ...
            ]
        ],
        // ...
    ],
    'fetched_at' => '2025-11-09 14:32:51',     // Время получения данных
    'hash' => 'b30f77e94cb7df27231380a90c7134b0' // MD5 хеш для отслеживания изменений
]
```

### Статусы hourly_status

- `on` - Свет присутствует (light_1, зеленый)
- `off` - Свет выключен (light_2, красный)
- `maybe` - Возможно отключение (light_3, желтый)

Каждый час представлен двумя элементами (2 получаса).

## Настройка

### 1. Переменные окружения

Добавьте в `.env`:

```env
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here
```

#### Как получить токен бота:

1. Найдите [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в `TELEGRAM_BOT_TOKEN`

#### Как получить Chat ID:

1. Добавьте вашего бота в чат или группу
2. Отправьте любое сообщение в этот чат
3. Откройте в браузере: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
4. Найдите `"chat":{"id":...}` и скопируйте это значение в `TELEGRAM_CHAT_ID`

### 2. Запуск миграции

```bash
php artisan migrate
```

### 3. Настройка планировщика

Система автоматически проверяет график каждые 10 минут через Laravel Scheduler.

Убедитесь, что в cron добавлена запись:

```bash
* * * * * cd /path-to-your-project && php artisan schedule:run >> /dev/null 2>&1
```

## Использование

### Ручное получение графика

```bash
# Получить график на сегодня
php artisan power:fetch-schedule

# Получить график на конкретную дату
php artisan power:fetch-schedule 08-11-2025
```

### Программное использование

```php
use App\Models\PowerOutageSchedule;

// Получить последний график
$schedule = PowerOutageSchedule::latest('fetched_at')->first();

// Получить график на конкретную дату
$schedule = PowerOutageSchedule::whereDate('schedule_date', '2025-11-08')
    ->latest('fetched_at')
    ->first();

// Получить все изменения за день
$changes = PowerOutageSchedule::whereDate('schedule_date', '2025-11-08')
    ->orderBy('fetched_at')
    ->get();

// Анализ расписания для конкретной очереди
foreach ($schedule->schedule_data as $row) {
    if ($row['queue'] === '1 черга' && $row['subqueue'] === '1') {
        $offHours = count(array_filter($row['hourly_status'], fn($s) => $s === 'off')) / 2;
        echo "Отключений: {$offHours} часов\n";
    }
}
```

### Тестирование отправки в Telegram

```php
use App\Jobs\SendPowerOutageNotification;
use App\Models\PowerOutageSchedule;

$schedule = PowerOutageSchedule::latest()->first();
SendPowerOutageNotification::dispatch($schedule);
```

## API эндпоинт

Источник данных: `https://www.poe.pl.ua/customs/newgpv-info.php`

Метод: `POST`

Параметры:
```
seldate={"date_in":"DD-MM-YYYY"}
```

## Структура файлов

```
app/
├── Console/Commands/
│   └── FetchPowerOutageSchedule.php    # Команда получения данных
├── Jobs/
│   └── SendPowerOutageNotification.php # Job для отправки уведомлений
├── Models/
│   └── PowerOutageSchedule.php         # Модель расписания
└── Services/
    └── PowerOutageParserService.php    # Сервис парсинга HTML

database/
└── migrations/
    └── *_create_power_outage_schedules_table.php

routes/
└── console.php                         # Настройка расписания
```

## Логирование

Все действия логируются:

```bash
# Просмотр логов
tail -f storage/logs/laravel.log | grep -i "power\|telegram"
```

## Troubleshooting

### Уведомления не отправляются

1. Проверьте настройки в `.env`
2. Убедитесь, что бот добавлен в чат
3. Проверьте логи: `storage/logs/laravel.log`
4. Проверьте очередь: `php artisan queue:work`

### Данные не обновляются

1. Проверьте работу планировщика: `php artisan schedule:list`
2. Запустите команду вручную: `php artisan power:fetch-schedule`
3. Проверьте доступность API: `curl -X POST "https://www.poe.pl.ua/customs/newgpv-info.php" -d 'seldate={"date_in":"08-11-2025"}'`

### Парсинг не работает

1. Сохраните HTML для анализа: `php artisan power:fetch-schedule > debug.html`
2. Проверьте структуру HTML на соответствие парсеру
3. Проверьте наличие ext-dom: `php -m | grep dom`

## Примеры уведомлений

Telegram сообщение при изменении графика:

```
🔌 Оновлення графіку відключень

📅 Дата: 08.11.2025

У зв'язку зі складною ситуацією в енергосистемі України, 
в Полтавській області 8 листопада 2025 року
з 07:00 по 16:00 запроваджений ГПВ в обсязі 2.5 черг.
з 16:00 по 23:59 запроваджений ГПВ в обсязі 4 черг.

⏰ Періоди:
• 07:00 - 16:00: 2.5 черг
• 16:00 - 23:59: 4 черг

📊 Черги:
• 1 черга: 9г вимк.
• 2 черга: 9.5г вимк.
• 3 черга: 9г вимк.
• 4 черга: 10г вимк.
• 5 черга: 6г вимк.
• 6 черга: 7г вимк.
```

## Дополнительные возможности

### Создание отчетов

```php
// Сравнение изменений за день
$schedules = PowerOutageSchedule::whereDate('schedule_date', today())
    ->orderBy('fetched_at')
    ->get();

foreach ($schedules as $i => $schedule) {
    echo "Версия " . ($i + 1) . " - " . $schedule->fetched_at . "\n";
    // Анализ различий...
}
```

### Webhook для получения данных

Можно создать endpoint для ручного триггера:

```php
Route::post('/api/power-outage/fetch', function() {
    Artisan::call('power:fetch-schedule');
    return response()->json(['status' => 'success']);
})->middleware('auth:sanctum');
```

## Поддержка

При возникновении проблем:

1. Проверьте документацию
2. Просмотрите логи
3. Запустите команду с флагом `-v` для подробного вывода
4. Создайте issue с описанием проблемы


# ⚡ ШВИДКИЙ СТАРТ - Автоматичні сповіщення про тривоги

## 🎯 Що це робить?

Система **автоматично**:
- ✅ Кожні 30 секунд перевіряє тривоги в Полтавській області ⚡
- ✅ Кожну хвилину перевіряє детально всі громади Полтавського району
- ✅ Відправляє повідомлення в Telegram при початку тривоги
- ✅ Відправляє повідомлення про відбій
- ✅ Зберігає історію у базі даних
- ✅ Щодня надсилає звіт о 20:00 та 21:00

## 🚀 Запуск за 3 кроки

### Крок 1: Налаштування (якщо ще не зроблено)

```bash
# 1. Додайте токени у .env
TELEGRAM_BOT_TOKEN=ваш_токен_від_BotFather
TELEGRAM_CHAT_ID=ваш_chat_id
AIR_ALERT_API_TOKEN=8a0343dfa946b66b0b4c7b6e6c1f867076ea1a74ab2203

# 2. Запустіть міграції
php artisan migrate
```

### Крок 2: Запустіть автоматичний скрипт

```bash
./scripts/start-air-alert-monitoring.sh
```

Цей скрипт:
- Перевірить конфігурацію
- Відправить тестове повідомлення в Telegram
- Запустить разовий моніторинг
- Покаже список запланованих завдань

### Крок 3: Додайте до crontab (для постійної роботи)

```bash
crontab -e
```

Додайте цей рядок:
```cron
* * * * * cd /home/igor/developer/task.famhub.local && php artisan schedule:run >> /dev/null 2>&1
```

**АБО** запустіть у фоновому режимі:
```bash
nohup php artisan schedule:work > /dev/null 2>&1 &
```

## ✅ Готово!

Тепер система працює **24/7** і відправлятиме сповіщення в Telegram!

## 📱 Приклад повідомлень

### При тривозі:
```
🚨 ПОВІТРЯНА ТРИВОГА!

📍 Регіон: Полтавська область
⚠️ Пройдіть до укриття!
```

### При відбої:
```
✅ Відбій повітряної тривоги

📍 Регіон: Полтавська область
```

### Щоденний звіт (о 20:00):
```
📊 Щоденний звіт

📅 11.11.2025

🚨 Кількість тривог: 3
⏱ Загальна тривалість: 2.5 год

Деталі:
1. 08:30 - 09:45 (1.3 год)
2. 14:20 - 15:50 (1.5 год)
3. 18:00 - 18:30 (0.5 год)
```

## 🔍 Перевірка роботи

### Подивитись логи

```bash
# Логи моніторингу
tail -f storage/logs/air-alert-monitor.log

# Логи Полтави
tail -f storage/logs/poltava-alerts.log

# Загальні логи Laravel
tail -f storage/logs/laravel.log
```

### Запустити вручну (для тесту)

```bash
# Перевірити всі області
php artisan air-alert:monitor

# Перевірити Полтаву
php artisan air-alert:monitor-poltava --all

# Згенерувати звіт
php artisan air-alert:daily-report
```

### Подивитись що заплановано

```bash
php artisan schedule:list
```

## 🛠️ Налаштування

### Змінити частоту перевірки

Відкрийте `routes/console.php` та змініть:

```php
// Кожні 30 секунд (поточне - максимально швидко) ⚡
Schedule::command('air-alert:monitor')
    ->everyThirtySeconds()

// Кожну 1 хвилину
Schedule::command('air-alert:monitor')
    ->everyMinute()

// Кожні 2 хвилини (економніше)
Schedule::command('air-alert:monitor')
    ->everyTwoMinutes()

// Кожні 5 хвилин (рідше)
Schedule::command('air-alert:monitor')
    ->everyFiveMinutes()
```

### Моніторити тільки конкретну область

```php
// routes/console.php

// Тільки Київ
Schedule::command('air-alert:monitor --region=31')
    ->everyTwoMinutes()
    ->runInBackground();
```

### Вимкнути моніторинг

Закоментуйте рядки в `routes/console.php`:

```php
// Schedule::command('air-alert:monitor')
//     ->everyTwoMinutes()
//     ...
```

## 🆘 Проблеми?

### Повідомлення не приходять

1. Перевірте токени:
```bash
php artisan tinker
config('services.telegram.bot_token')
config('services.telegram.chat_id')
exit
```

2. Відправте тестове повідомлення:
```bash
php artisan tinker
$telegram = new \App\Services\TelegramService();
$telegram->sendMessage('Тест');
exit
```

3. Перегляньте логи:
```bash
grep -i "telegram" storage/logs/laravel.log | tail -20
```

### Scheduler не працює

1. Перевірте crontab:
```bash
crontab -l
```

2. Запустіть вручну:
```bash
php artisan schedule:run
```

3. Подивіться що виконується:
```bash
php artisan schedule:list
```

## 📚 Документація

- **AIR-ALERT-NOTIFICATIONS-SETUP.md** - Детальна інструкція
- **FINAL-AIR-ALERT-SUMMARY.md** - Загальний огляд системи
- **QUICKSTART-AIR-ALERT.md** - Швидкий старт з API

## 🎓 Додаткові можливості

### Відправка звітів на email

```php
// Додайте у routes/console.php
Schedule::command('air-alert:daily-report')
    ->dailyAt('20:00')
    ->emailOutputTo('admin@example.com');
```

### Сповіщення в Slack

```php
// Додайте у routes/console.php
Schedule::command('air-alert:monitor')
    ->everyTwoMinutes()
    ->onFailure(function () {
        // Slack notification
    });
```

### Webhook для інших систем

Створіть API endpoint у `routes/api.php`:

```php
Route::post('/webhooks/air-alert', function (Request $request) {
    // Обробка webhook
});
```

---

**Система готова! Слава Україні! 🇺🇦**


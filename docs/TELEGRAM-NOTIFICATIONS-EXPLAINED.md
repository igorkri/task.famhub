# 📱 Коли відправляються повідомлення в Telegram

## ⚡ Відповідь: ТІЛЬКИ при зміні статусу!

### ✅ Коли ВІДПРАВИТЬ повідомлення:

#### 1. При початку тривоги
```
Статус БУВ: Немає тривоги (або перший запуск)
Статус СТАВ: Є тривога
→ 🚨 ВІДПРАВИТЬ повідомлення в Telegram!
```

**Повідомлення:**
```
🚨 ПОВІТРЯНА ТРИВОГА!

📍 Регіон: Полтавська область
⚠️ Пройдіть до укриття!
```

#### 2. При відбої тривоги
```
Статус БУВ: Є тривога
Статус СТАВ: Немає тривоги
→ ✅ ВІДПРАВИТЬ повідомлення в Telegram!
```

**Повідомлення:**
```
✅ Відбій повітряної тривоги

📍 Регіон: Полтавська область
```

---

### ❌ Коли НЕ ВІДПРАВИТЬ повідомлення:

#### 1. Тривоги немає і не було
```
Статус БУВ: Немає тривоги
Статус СТАВ: Немає тривоги
→ ℹ️ Статус не змінився - повідомлення НЕ відправляється
```

**Що побачите в консолі:**
```
🔍 Перевірка статусу повітряних тривог...
ℹ️ Статус не змінився для регіону Полтавська область (тривоги немає)
```

#### 2. Тривога триває
```
Статус БУВ: Є тривога
Статус СТАВ: Є тривога
→ ℹ️ Статус не змінився - повідомлення НЕ відправляється
```

**Що побачите в консолі:**
```
🔍 Перевірка статусу повітряних тривог...
ℹ️ Статус не змінився для регіону Полтавська область (активна тривога)
```

---

## 🔄 Як це працює

### Механізм відстеження змін:

1. **При кожному запуску команда:**
   - Отримує поточний статус з API
   - Дивиться попередній статус в Cache
   - Порівнює їх

2. **Якщо статуси різні:**
   - Зберігає новий статус в Cache
   - Зберігає в БД (таблиця `air_alerts`)
   - **ВІДПРАВЛЯЄ повідомлення в Telegram** 📱

3. **Якщо статуси однакові:**
   - Просто показує інфо в консолі
   - Нічого не відправляє

---

## 📊 Приклад роботи

### День без тривог:

```bash
# 1-й запуск (15:00:00)
php artisan air-alert:monitor --region=19
→ ℹ️ Статус не змінився (тривоги немає)
→ Повідомлення НЕ відправлено

# 2-й запуск (15:00:30) - через 30 сек
php artisan air-alert:monitor --region=19
→ ℹ️ Статус не змінився (тривоги немає)
→ Повідомлення НЕ відправлено

# 3-й запуск (15:01:00) - через 1 хв
php artisan air-alert:monitor --region=19
→ ℹ️ Статус не змінився (тривоги немає)
→ Повідомлення НЕ відправлено

...і так далі, поки не з'явиться тривога
```

**Результат:** 0 повідомлень в Telegram (це нормально!)

---

### День з тривогою:

```bash
# 15:00:00 - Тривоги немає
php artisan air-alert:monitor --region=19
→ ℹ️ Тривоги немає
→ Повідомлення НЕ відправлено

# 15:00:30 - Тривоги немає
php artisan air-alert:monitor --region=19
→ ℹ️ Тривоги немає
→ Повідомлення НЕ відправлено

# 15:01:00 - З'явилась тривога! 🚨
php artisan air-alert:monitor --region=19
→ 🚨 ТРИВОГА! Регіон: Полтавська область
→ 📱 ВІДПРАВЛЕНО повідомлення в Telegram:
   "🚨 ПОВІТРЯНА ТРИВОГА!
    📍 Регіон: Полтавська область
    ⚠️ Пройдіть до укриття!"

# 15:01:30 - Тривога продовжується
php artisan air-alert:monitor --region=19
→ ℹ️ Статус не змінився (активна тривога)
→ Повідомлення НЕ відправлено

# 15:02:00 - Тривога продовжується
php artisan air-alert:monitor --region=19
→ ℹ️ Статус не змінився (активна тривога)
→ Повідомлення НЕ відправлено

...

# 15:45:00 - Відбій! ✅
php artisan air-alert:monitor --region=19
→ ✅ Відбій тривоги. Регіон: Полтавська область
→ 📱 ВІДПРАВЛЕНО повідомлення в Telegram:
   "✅ Відбій повітряної тривоги
    📍 Регіон: Полтавська область"

# 15:45:30 - Тривоги немає
php artisan air-alert:monitor --region=19
→ ℹ️ Тривоги немає
→ Повідомлення НЕ відправлено
```

**Результат:** 2 повідомлення в Telegram (початок + відбій)

---

## 🧪 Як протестувати

### Спосіб 1: Симуляція зміни статусу

```bash
# Очистити кеш (скинути попередній статус)
php artisan cache:forget air_alert_status_19

# Запустити команду
php artisan air-alert:monitor --region=19

# Якщо зараз є тривога - відправить повідомлення
# Якщо немає тривоги - відправить тільки при наступній зміні
```

### Спосіб 2: Тестове повідомлення напряму

```bash
php artisan tinker
```

```php
use App\Services\TelegramService;

$telegram = new TelegramService();

// Тест повідомлення про тривогу
$telegram->sendMessage(
    '🚨 <b>ТЕСТОВА ТРИВОГА!</b>' . "\n\n" .
    '📍 Регіон: <b>Полтавська область</b>' . "\n" .
    '⚠️ <i>Це тест системи!</i>'
);

// Тест повідомлення про відбій
$telegram->sendMessage(
    '✅ <b>ТЕСТОВИЙ ВІДБІЙ</b>' . "\n\n" .
    '📍 Регіон: <b>Полтавська область</b>'
);

exit
```

---

## 📋 Чому так зроблено?

### Переваги системи з відстеженням змін:

✅ **Не спамить** - якщо тривога триває годину, не відправить 120 повідомлень  
✅ **Економить ресурси** - не перевантажує Telegram  
✅ **Інформативно** - кожне повідомлення важливе  
✅ **Зручно** - не треба відключати сповіщення  

### Якби відправляли завжди:

❌ **Проблема 1:** Спам
```
15:00 - 🚨 Тривога
15:00:30 - 🚨 Тривога
15:01 - 🚨 Тривога
15:01:30 - 🚨 Тривога
...
120 повідомлень за годину! 😱
```

❌ **Проблема 2:** Неможливо зрозуміти коли почалась/закінчилась

❌ **Проблема 3:** Telegram може заблокувати за спам

---

## 🔍 Як дізнатись поточний статус?

### Варіант 1: Запустити команду вручну

```bash
php artisan air-alert:monitor --region=19
```

Побачите:
- `ℹ️ Тривоги немає` - все спокійно
- `ℹ️ Активна тривога` - зараз тривога

### Варіант 2: Подивитись в БД

```bash
php artisan tinker
```

```php
use App\Models\AirAlert;

// Активні тривоги зараз
$active = AirAlert::active()->get();
echo "Активних тривог: " . $active->count() . "\n";

// Для Полтавської області
$poltava = AirAlert::forRegion('19')->active()->first();
if ($poltava) {
    echo "🚨 Тривога у Полтавській області!\n";
    echo "Почалась: " . $poltava->started_at . "\n";
} else {
    echo "✅ Тривоги немає\n";
}

exit
```

### Варіант 3: Перевірити API напряму

```bash
php artisan tinker
```

```php
$airAlert = new \App\Services\AirAlertService();
$status = $airAlert->getAlertByRegion('19');
print_r($status);
exit
```

---

## 💡 Підсумок

### Повідомлення відправляються:

1. ✅ **При початку тривоги** - одне повідомлення 🚨
2. ✅ **При відбої** - одне повідомлення ✅
3. ❌ **НЕ відправляються** якщо статус не змінився

### Частота перевірки:

- Команда запускається **кожні 30 секунд** (через scheduler)
- Але повідомлення - **ТІЛЬКИ при зміні!**

### Це нормально:

- ✅ Запустили команду - немає повідомлення (тривог немає)
- ✅ Запустили 10 разів - немає повідомлень (статус не змінився)
- ✅ З'явилась тривога - **ПРИЙШЛО** повідомлення! 🚨
- ✅ Відбій - **ПРИЙШЛО** повідомлення! ✅

---

**Система працює правильно! 🎯**

**Слава Україні! 🇺🇦**


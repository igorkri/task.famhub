# 🔔 Налаштування автоматичних сповіщень про тривоги

## Що вже налаштовано ✅

У файлі `routes/console.php` вже додано автоматичний моніторинг:

### 1. Моніторинг Полтавської області
- **Частота:** Кожні 30 секунд ⚡
- **Команда:** `air-alert:monitor --region=19`
- **Що робить:** Перевіряє статус тривоги в Полтавській області і відправляє сповіщення в Telegram при зміні статусу

### 2. Детальний моніторинг громад Полтави
- **Частота:** Кожну хвилину
- **Команда:** `air-alert:monitor-poltava --all`
- **Що робить:** Показує детальну інформацію про тривоги в конкретних громадах Полтавської області

### 3. Щоденні звіти
- **Загальний звіт:** Щодня о 20:00 (`air-alert:daily-report`)
- **Звіт по Полтаві:** Щодня о 21:00 (`air-alert:daily-report --region=19`)

## 🚀 Як запустити

### Варіант 1: Через Laravel Scheduler (Рекомендовано)

#### Крок 1: Додати в crontab

```bash
crontab -e
```

Додайте цей рядок:
```cron
* * * * * cd /home/igor/developer/task.famhub.local && php artisan schedule:run >> /dev/null 2>&1
```

Це запустить scheduler Laravel кожну хвилину, а він вже сам буде викликати потрібні команди у правильний час.

#### Крок 2: Перевірити що працює

```bash
# Подивитись список запланованих завдань
php artisan schedule:list

# Запустити вручну (для тесту)
php artisan schedule:run

# Подивитись логи
tail -f storage/logs/air-alert-monitor.log
tail -f storage/logs/poltava-alerts.log
```

### Варіант 2: Через Supervisor (для продакшн)

Створіть файл `/etc/supervisor/conf.d/air-alert-scheduler.conf`:

```ini
[program:air-alert-scheduler]
process_name=%(program_name)s
command=php /home/igor/developer/task.famhub.local/artisan schedule:work
directory=/home/igor/developer/task.famhub.local
user=www-data
autostart=true
autorestart=true
redirect_stderr=true
stdout_logfile=/var/log/air-alert-scheduler.log
stopwaitsecs=60
```

Потім:
```bash
sudo supervisorctl reread
sudo supervisorctl update
sudo supervisorctl start air-alert-scheduler
```

### Варіант 3: Ручний запуск для тесту

```bash
# Одноразова перевірка всіх областей
php artisan air-alert:monitor

# Одноразова перевірка Полтави
php artisan air-alert:monitor-poltava --all

# Згенерувати звіт зараз
php artisan air-alert:daily-report
```

## 📊 Як це працює

### Сценарій 1: Початок тривоги

1. **Scheduler запускає** `air-alert:monitor` кожні 2 хвилини
2. **Команда перевіряє** статус через API alerts.in.ua
3. **Виявляє зміну:** у Полтавській області з'явилась тривога
4. **Відправляє Job:** `SendAirAlertNotification::dispatch()`
5. **Job відправляє** повідомлення в Telegram через `TelegramService`
6. **Зберігає в БД:** новий запис у таблиці `air_alerts`

**Результат в Telegram:**
```
🚨 ПОВІТРЯНА ТРИВОГА!

📍 Регіон: Полтавська область
⚠️ Пройдіть до укриття!
```

### Сценарій 2: Відбій тривоги

1. **Scheduler запускає** команду через 2 хвилини
2. **Виявляє зміну:** тривога закінчилась
3. **Відправляє сповіщення** про відбій
4. **Оновлює БД:** встановлює `ended_at` та розраховує тривалість

**Результат в Telegram:**
```
✅ Відбій повітряної тривоги

📍 Регіон: Полтавська область
```

### Сценарій 3: Детальний моніторинг Полтави

**Команда:** `air-alert:monitor-poltava --all` (кожні 3 хвилини)

**Якщо є тривоги, в Telegram:**
```
🚨 ТРИВОГА у Полтавській області!

Активних тривог: 2

📍 Диканьська територіальна громада
🚨 Повітряна тривога

📍 м. Полтава та Полтавська територіальна громада
🚨 Повітряна тривога
```

### Сценарій 4: Щоденний звіт (о 20:00)

**Результат в Telegram:**
```
📊 Щоденний звіт

📅 11.11.2025
📍 Регіон: Полтавська область

🚨 Кількість тривог: 3
⏱ Загальна тривалість: 2.5 год

Деталі:
1. 08:30 - 09:45 (1.3 год)
2. 14:20 - 15:50 (1.5 год)
3. 18:00 - 18:30 (0.5 год)
```

## 🔧 Налаштування частоти моніторингу

Відкрийте `routes/console.php` та змініть частоту:

```php
// Кожні 30 секунд (поточне налаштування - максимально швидко) ⚡
Schedule::command('air-alert:monitor')
    ->everyThirtySeconds()

// Кожну 1 хвилину
Schedule::command('air-alert:monitor')
    ->everyMinute()

// Кожні 2 хвилини (більш економно)
Schedule::command('air-alert:monitor')
    ->everyTwoMinutes()

// Кожні 5 хвилин (економія API запитів)
Schedule::command('air-alert:monitor')
    ->everyFiveMinutes()

// Кожні 10 хвилин
Schedule::command('air-alert:monitor')
    ->everyTenMinutes()
```

**Рекомендація:** 30 секунд - оптимально для швидкого реагування на тривоги без перевантаження API.

## 📝 Логування

Всі запуски логуються у окремі файли:

```bash
# Загальний моніторинг
tail -f storage/logs/air-alert-monitor.log

# Полтавська область
tail -f storage/logs/poltava-alerts.log

# Laravel логи (помилки)
tail -f storage/logs/laravel.log
```

## ⚙️ Додаткові налаштування

### Моніторинг тільки конкретних областей

Якщо вам потрібен моніторинг тільки певних областей, створіть окрему команду:

```php
// routes/console.php

// Тільки Київ
Schedule::command('air-alert:monitor --region=31')
    ->everyTwoMinutes()
    ->withoutOverlapping()
    ->runInBackground();

// Тільки Харків
Schedule::command('air-alert:monitor --region=22')
    ->everyTwoMinutes()
    ->withoutOverlapping()
    ->runInBackground();
```

### Відправка тільки розробнику (тестування)

У Job `SendAirAlertNotification` вже є параметр `sendToDev: true`, який дублює повідомлення розробнику (chat_id: 841714438).

## 🧪 Тестування

### 1. Перевірити що scheduler працює

```bash
# Запустити scheduler вручну
php artisan schedule:run

# Подивитись що виконалось
tail -20 storage/logs/air-alert-monitor.log
```

### 2. Симуляція зміни статусу

```bash
# Запустити команду вручну
php artisan air-alert:monitor --region=19

# Якщо статус не змінився, побачите:
# ℹ️ Статус не змінився для регіону Полтавська область
```

### 3. Перевірити Telegram

Команда `SendAirAlertNotification` автоматично відправить повідомлення при зміні статусу.

## 🚨 Усунення проблем

### Повідомлення не приходять в Telegram

1. **Перевірте токен бота:**
```bash
php artisan tinker
config('services.telegram.bot_token')
config('services.telegram.chat_id')
exit
```

2. **Протестуйте вручну:**
```bash
php artisan tinker
$telegram = new \App\Services\TelegramService();
$telegram->sendMessage('🧪 Тест');
exit
```

3. **Перегляньте логи:**
```bash
grep -i "telegram" storage/logs/laravel.log | tail -20
```

### Scheduler не запускається

1. **Перевірте crontab:**
```bash
crontab -l
```

2. **Запустіть вручну:**
```bash
php artisan schedule:run
```

3. **Перевірте що команди існують:**
```bash
php artisan list | grep air-alert
```

### Команди виконуються, але статус не змінюється

Це нормально! Команда відправить повідомлення **ТІЛЬКИ** при зміні статусу:
- Не було тривоги → з'явилась тривога = **відправить повідомлення**
- Тривога була → тривога є = **не відправить** (статус не змінився)
- Тривога була → відбій = **відправить повідомлення**

## 📋 Чеклист налаштування

- [ ] Додано токени у `.env`
- [ ] Запущено міграції (`php artisan migrate`)
- [ ] Додано завдання в crontab
- [ ] Протестовано Telegram (`php artisan tinker`)
- [ ] Протестовано команди (`php artisan air-alert:monitor`)
- [ ] Перевірено логи (`tail -f storage/logs/air-alert-monitor.log`)
- [ ] Запущено scheduler (`php artisan schedule:run`)

## ✅ Готово!

Після налаштування crontab, система **автоматично** буде:

✅ Кожні 2 хвилини перевіряти всі області України  
✅ Кожні 3 хвилини перевіряти громади Полтави  
✅ Відправляти повідомлення в Telegram при тривозі  
✅ Відправляти повідомлення про відбій  
✅ Щодня о 20:00 надсилати звіт  
✅ Зберігати всю історію у базі даних  

**Система працює 24/7 в автоматичному режимі! 🚀**

---

**Слава Україні! 🇺🇦**

